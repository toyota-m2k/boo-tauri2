{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-capability",
  "description": "Capabilities only enabled on mobile (iOS / Android)",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": [
    "barcode-scanner:default"
  ]
}
